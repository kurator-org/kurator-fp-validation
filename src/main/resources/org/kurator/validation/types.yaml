imports:

- classpath:/org/kurator/akka/types.yaml

types:

- id: NativePythonActor
  className: org.kurator.akka.PythonActorConfig
  singleton: true
  properties:
    actorClass: org.kurator.validation.actors.NativePythonActor